Remoções e desativações realizadas em 2025-11-04

Resumo
- Removido suporte a 'moedinhas' no frontend e backend.
- Desativadas e removidas rotas/páginas relacionadas a prêmios e resgates no frontend.
- Atualizados componentes de ranking, administração e wizard para usar somente pontos (R$).
- Ajustado seed do backend para remover casts `as any` e compatibilizar com o schema atual do Prisma.
- KPI de "Resgates Solicitados" removido do painel Admin.

Arquivos removidos (frontend)
- frontend/src/app/(dashboard)/premios/page.tsx
- frontend/src/app/(dashboard)/admin/premios/page.tsx
- frontend/src/app/(dashboard)/admin/catalogo-premios/page.tsx
- frontend/src/app/(dashboard)/admin/resgates/page.tsx

Arquivos/alterados importantes
- frontend/src/config/menuItems.ts -> menu atualizado (itens de prêmios/resgates removidos)
- frontend/src/components/dashboard/KpisAdmin.tsx -> KPI de Resgates removido
- frontend/src/... (vários) -> remoção de menções a 'moedinhas' (comentários e textos)
- backend/prisma/seed.ts -> removidos casts `as any` e limpeza do processo de seed

Observações e próximos passos
- As alterações no banco de dados (Prisma) já foram aplicadas anteriormente via `prisma db push` pelo usuário; se for necessário gerar migrations formais, devemos fazê-lo com cuidado (opção adiada até confirmação).
- Recomendo rodar os testes/linters locais e revisar se há endpoints backend que ainda retornam campos antigos (caso existam integrações externas).

Como reverter (rápido)
- Se quiser restaurar quaisquer arquivos deletados, eu posso reverter via git (se estiver em um commit) ou re-criar os arquivos manualmente.

Feito por: alterações automáticas de limpeza (sobre solicitação do usuário)
Data: 2025-11-04
