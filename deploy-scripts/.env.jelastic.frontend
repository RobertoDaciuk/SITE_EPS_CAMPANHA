# ============================================================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO JELASTIC (FRONTEND)
# ============================================================================
# Copie este arquivo e configure no painel Jelastic:
# Settings → Variables → Adicionar cada variável abaixo
# ============================================================================

# ----------------------------------------------------------------------------
# API BACKEND
# ----------------------------------------------------------------------------
# URL PÚBLICA do backend (com HTTPS após configurar domínio)
NEXT_PUBLIC_API_URL=https://api.seudominio.com/api

# URL INTERNA (antes de configurar domínio customizado)
# NEXT_PUBLIC_API_URL=http://node<ID_BACKEND>-eps-campanhas-prod.jelastic.saveincloud.net/api

# ----------------------------------------------------------------------------
# ENVIRONMENT
# ----------------------------------------------------------------------------
NODE_ENV=production
PORT=3001

# ----------------------------------------------------------------------------
# TIMEZONE
# ----------------------------------------------------------------------------
TZ=America/Sao_Paulo

# ----------------------------------------------------------------------------
# NEXT.JS OPTIMIZATIONS
# ----------------------------------------------------------------------------
# Desabilitar telemetria do Next.js (opcional)
NEXT_TELEMETRY_DISABLED=1

# ----------------------------------------------------------------------------
# OPCIONAIS
# ----------------------------------------------------------------------------

# Analytics (se usar Google Analytics, Mixpanel, etc)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_MIXPANEL_TOKEN=seu-token

# Feature Flags (se implementar)
# NEXT_PUBLIC_ENABLE_BETA_FEATURES=false

# API Keys públicas (se necessário)
# NEXT_PUBLIC_MAPBOX_TOKEN=seu-token
# NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_live_xxx

# ============================================================================
# INSTRUÇÕES DE CONFIGURAÇÃO NO JELASTIC
# ============================================================================
# 
# 1. Acesse o painel Jelastic
# 2. Clique no nó Frontend
# 3. Vá em "Config" → "Variables"
# 4. Clique em "Add" para cada variável
# 5. Copie o nome da variável (ex: NEXT_PUBLIC_API_URL)
# 6. Cole o valor correspondente
# 7. IMPORTANTE: Variáveis NEXT_PUBLIC_* são embutidas no build
#    Após alterar, é necessário fazer rebuild: npm run build
# 8. Clique em "Save" após adicionar todas
# 9. Restart o nó
#
# ============================================================================

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
#
# ⚠️  VARIÁVEIS NEXT_PUBLIC_*:
#    - São expostas publicamente no browser
#    - Nunca coloque secrets/senhas nelas
#    - São embutidas durante o build (não em runtime)
#    - Mudanças requerem novo build
#
# 🔐 VARIÁVEIS PRIVADAS (sem NEXT_PUBLIC_):
#    - Só acessíveis no servidor (API Routes, getServerSideProps)
#    - Use para secrets, chaves privadas, etc.
#
# 🌐 URLS:
#    - Sempre use HTTPS em produção
#    - Não inclua barra final na URL da API
#
# ============================================================================
