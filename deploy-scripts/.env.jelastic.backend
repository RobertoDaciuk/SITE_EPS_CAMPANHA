# ============================================================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO JELASTIC (BACKEND)
# ============================================================================
# Copie este arquivo e configure no painel Jelastic:
# Settings → Variables → Adicionar cada variável abaixo
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# ----------------------------------------------------------------------------
DATABASE_URL=postgresql://eps_user:SUA_SENHA_FORTE@node<ID>-eps-campanhas-prod.jelastic.saveincloud.net:5432/eps_campanhas?schema=public

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------
PORT=3000
NODE_ENV=production

# ----------------------------------------------------------------------------
# JWT AUTHENTICATION
# ----------------------------------------------------------------------------
# IMPORTANTE: Gere uma chave forte de 64+ caracteres
# PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))
# Linux: openssl rand -base64 64
JWT_SECRET=SUBSTITUA_POR_CHAVE_SECRETA_MUITO_FORTE_64_CARACTERES_MINIMO
JWT_EXPIRES_IN=7d

# ----------------------------------------------------------------------------
# CORS
# ----------------------------------------------------------------------------
# URL do frontend (será atualizado após configurar domínio)
FRONTEND_URL=https://app.seudominio.com

# ----------------------------------------------------------------------------
# FILE UPLOADS
# ----------------------------------------------------------------------------
UPLOAD_DIR=/var/www/webroot/ROOT/public/uploads
MAX_FILE_SIZE=5242880

# ----------------------------------------------------------------------------
# RATE LIMITING
# ----------------------------------------------------------------------------
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# ----------------------------------------------------------------------------
# TIMEZONE
# ----------------------------------------------------------------------------
TZ=America/Sao_Paulo

# ----------------------------------------------------------------------------
# OPCIONAIS (configurar se necessário)
# ----------------------------------------------------------------------------

# Email (SMTP) - Descomente e configure se necessário
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-de-app
# SMTP_FROM=noreply@seudominio.com

# AWS S3 (alternativa ao upload local) - Descomente se usar S3
# AWS_ACCESS_KEY_ID=sua-access-key
# AWS_SECRET_ACCESS_KEY=sua-secret-key
# AWS_REGION=us-east-1
# AWS_BUCKET_NAME=eps-campanhas-uploads

# Logs e Monitoramento
# SENTRY_DSN=https://seu-sentry-dsn@sentry.io/projeto
# LOG_LEVEL=info

# ============================================================================
# INSTRUÇÕES DE CONFIGURAÇÃO NO JELASTIC
# ============================================================================
# 
# 1. Acesse o painel Jelastic
# 2. Clique no nó Backend
# 3. Vá em "Config" → "Variables"
# 4. Clique em "Add" para cada variável
# 5. Copie o nome da variável (ex: DATABASE_URL)
# 6. Cole o valor correspondente
# 7. Clique em "Save" após adicionar todas
# 8. Restart o nó
#
# ============================================================================
